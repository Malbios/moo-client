{
  "name": "moo-client-ts",
  "version": "0.3.0",
  "description": "A utility client for MOO servers written in TypeScript",
  "keywords": [
    "MOO",
    "LambdaMOO",
    "ToastStunt"
  ],
  "author": "Malbios",
  "license": "ISC",
  "bugs": "https://github.com/Malbios/moo-client-ts/issues",
  "repository": "https://github.com/Malbios/moo-client-ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "deploy:prod": "npm run test && npm run version:minor && npm publish",
    "compile": "npx tsc",
    "clean": "rimraf dist",
    "build": "npm run clean && npm run compile",
    "prepublish": "npm run build && npm run test",
    "pretest": "npm run build",
    "test": "ts-node node_modules/jasmine/bin/jasmine --config=jasmine.json",
    "version:minor": "npm version minor && git tag && git push --tags origin HEAD:main"
  },
  "dependencies": {
    "@types/node": "^20.12.8",
    "net": "^1.0.2",
    "telnet-stream": "^1.1.0",
    "yaml": "^2.4.2"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.4",
    "jasmine": "^5.1.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
